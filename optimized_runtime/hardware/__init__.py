# Hardware Abstraction Layer Package

This module will eventually contain:
- Camera abstraction layer
- Microphone abstraction layer  
- Speaker abstraction layer
- LiDAR abstraction layer
- Motor control abstraction
- GPIO abstraction for LED, buttons, etc.

Currently delegated to service adapters that wrap existing robot HAL.
