# NovaCare Robot Service — Dependencies
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# Image processing (lightweight JPEG encode/resize — no OpenCV)
Pillow>=10.0.0

# Text-to-Speech
gTTS>=2.3.0

# Speech-to-Text
SpeechRecognition>=3.10.0
PyAudio>=0.2.14

# NOTE: The `pop` library is pre-installed on the SERBot SBC.
# It is NOT available on PyPI — do not list it here.

# NOTE: OpenCV was removed. Camera capture now uses V4L2 + Pillow.
# Install v4l-utils on the robot: sudo apt install v4l-utils
