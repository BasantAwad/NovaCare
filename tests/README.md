# Tests

Central location for all automated tests.

Current policy (non-destructive refactor):
- Copy existing tests first into `tests/`.
- Prefer categorization by intent (unit/integration/hardware/AI/experimental).
- Avoid removing tests until coverage and CI safety are verified.

